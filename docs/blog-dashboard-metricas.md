# Blog Dashboard – Métricas e Datas

Ajustes aplicados:
- Coluna "Criado em" passou a exibir `published_at` (quando houver) com fallback para `created_at`.
- `views_count` continua sendo exibido a partir do campo da tabela, coerente com o incremento via RPC no Post.

Se necessário, podemos adicionar cartões de estatísticas específicas por período em uma iteração futura.
